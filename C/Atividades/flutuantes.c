#include <stdio.h>


int main(){

    int a = 10;
    float b = 5.9;
    float resultado = a + b;

    int s = 10;
    int d = 3;
    float quociente = (float) s / d;

    printf("Quociente: %.2f\n", quociente);
    printf("Resultado: %.2f\n", resultado);

   //float soma = x + y;
   // float diferenca = x - y;
   // float produto = x * y;
   // float quociente = x / y;

    //printf("Soma: %.2f\n", soma);
    //printf("Diferença: %.2f\n", diferenca);
    //printf("Produto: %.2f\n", produto);
    //printf("Quociente: %.2f\n", quociente);




    return 0;
}